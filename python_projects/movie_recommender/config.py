"""
Configuration file for the Movie Recommender application.

Contains essential constants for:
1. JSON file storing movie data (FILE_NAME).
2. Log file name (LOG_FILE).
3. Logger instance name (LOGGER_NAME).

These constants are imported in other modules to maintain consistency
across the project.
"""

FILE_NAME = 'movies.json'
LOG_FILE = 'movies_app.log'
LOGGER_NAME = 'MovieRecommender'